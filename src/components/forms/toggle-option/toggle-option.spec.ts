import { ToggleOption } from './toggle-option';

it('should render and respond to changes appropriately', () => {
    const toggleOption = new ToggleOption();
    expect(toggleOption).toBeInstanceOf(ToggleOption);
});
