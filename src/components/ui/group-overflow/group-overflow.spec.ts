import { GroupOverflow } from './group-overflow';

it('should render and respond to changes appropriately', () => {
  const groupOverflow = new GroupOverflow();
  expect(groupOverflow).toBeInstanceOf(GroupOverflow);
});
